# Copy this file to `.env` and fill in your key.
# `.env` is gitignored — the key never leaves this machine.

# Anthropic API key. Used for all four Claude touchpoints:
# resume parsing, job scoring, screening-question mapping, tailored blurb.
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
