import { createStubAdapter } from "./stub.mts";

/** Not built yet — see stub.mts for why, and naukri.mts for what a real one looks like. */
export const instahyreAdapter = createStubAdapter("instahyre");
